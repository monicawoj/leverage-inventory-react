import styled from 'styled-components';

const StyledDescriptionHolder = styled.div`
  width: 100%;
  //clear: both;
  p {
    font-size: 16px;
    margin-bottom: 20px;
  }
`;

export default StyledDescriptionHolder;
